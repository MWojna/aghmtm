#include <stm32f429i_discovery_lcd.h>
#include "LedLcd.h"

LedLcd::LedLcd() {
	BSP_LCD_Init();
}



void LedLcd::On(unsigned int Button) {
	if(Button == 4) {
	} else {
	BSP_LCD_SetTextColor(LCD_COLOR_GREEN);
	BSP_LCD_FillRect(0, Button*80, 80, 80);
	}
}








